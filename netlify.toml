[build]
  publish = "dist"
  command = "npm run build"

[context.production]
  site = "nfp_ccsFPmt165aa1zwVVSM8JgENK5EdnPA312ff"

# Note: We use server-side meta tags via Helmet, but bots need server-rendered HTML
# For social media bots, meta tags are handled by React Helmet in the client
# For better SEO, ensure all pages include PaymentMetaTags component

# Default redirect for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Netlify Forms Configuration
# Email notifications will be sent to the email configured in Netlify dashboard
# Go to Site Settings > Forms > Form notifications to configure Gmail notifications
